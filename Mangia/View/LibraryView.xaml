<UserControl x:Class="Mangia.View.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="300" MinWidth="400"
             VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
    <Grid Background="LightBlue">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding MangaList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" Margin="20" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="8" Padding="6" BorderBrush="#DDD" BorderThickness="1" Background="#FFF"
                                Width="160" Height="220"
                                MouseLeftButtonUp="OnLibraryItemClick" Cursor="Hand">
                            <StackPanel>
                                <Image x:Name="coverImage"
                                       Source="{Binding CoverUrl}"
                                       Height="120"
                                       Stretch="UniformToFill"
                                       Margin="0,0,0,8" />
                                <TextBlock Text="{Binding Title}"
                                           TextAlignment="Center"
                                           FontWeight="Bold"
                                           FontSize="16" />
                            </StackPanel>
                        </Border>
                        <DataTemplate.Resources>
                            <Style TargetType="Image">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CoverUrl}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Title}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataTemplate.Resources>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>